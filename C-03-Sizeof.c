#include<stdio.h>
int main()
{
    char ch;
    int i;
    double j;
    float k;

    // I wan to print the size of every data-type

    printf("Size of Integer data-type is: %d \n", sizeof(i));

        printf("Size of Character data-type is: %d\n", sizeof(ch));

            printf("Size of Float data-type is: %d\n", sizeof(k));

                printf("Size of Double data-type is: %d\n", sizeof(j));




    return 0;
}
